structure CPSTestBasic = struct

open Basic
       
val a = true

(* fun f (n : int) = () *)
                    
val b = case a of
            true => (fn (n : int) => ()) 1
          | false => (fn (n : int) => ()) 0
          
(* val b = case true of *)
(*             true => 10 + 20 *)
(*           | false => 30 + 40 *)

val b = case a of
            true => 10 + 20
          | false => 30 + 40

val c = b + 1 + 2 + 3 + b

fun f () = true
             
val neg1 =
 fn b =>
    case b of
        true => false
      | false => true

fun neg b =
    case b of
        true => false
      | false => true

val a = neg a

end
